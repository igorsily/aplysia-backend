// import moment from 'moment';

// function dateFormat(data: string, format: string) {
//   return 'moment(data).format(format)';
// }

function dateFormat() {
  //   return 'moment(data).format(format)';
}
function booleanFormat(value: boolean) {
  return value ? 'SIM' : 'NÃO';
}

export { dateFormat, booleanFormat };
